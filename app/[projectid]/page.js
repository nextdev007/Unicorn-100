"use client";
import React from "react";

const SingleProject = ({ projectid }) => {
  console.log(projectid);
  return <div>{`i am single project ${projectid}`}</div>;
};

export default SingleProject;
